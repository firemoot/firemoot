name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  scala:
    name: server (Scala)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up toolchain (mise)
        uses: jdx/mise-action@v4
        with:
          cache: true

      - name: Cache Coursier and sbt
        uses: actions/cache@v5
        with:
          path: |
            ~/.cache/coursier
            ~/.sbt
          key: sbt-${{ runner.os }}-${{ hashFiles('build.sbt', 'project/**', '.scalafmt.conf') }}
          restore-keys: sbt-${{ runner.os }}-

      - name: Format check, compile and test
        run: sbt -batch scalafmtCheckAll scalafmtSbtCheck test

  sdk:
    name: sdk (TypeScript)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up toolchain (mise)
        uses: jdx/mise-action@v4
        with:
          cache: true

      - name: Cache pnpm store
        uses: actions/cache@v5
        with:
          path: ~/.local/share/pnpm/store
          key: pnpm-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: pnpm-${{ runner.os }}-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Format check
        run: pnpm run format:check

      # Build first: @firemoot/client/@firemoot/test resolve @firemoot/core via its
      # built dist (.d.ts + .js), so typecheck and test need it compiled.
      - name: Build
        run: pnpm -r build

      - name: Typecheck
        run: pnpm -r typecheck

      - name: Test
        run: pnpm -r test

      - name: Fail on admin SPA drift
        run: |
          git add -A -- server/src/main/resources/admin
          if ! git diff --cached --exit-code -- server/src/main/resources/admin; then
            echo "::error::Admin SPA build output is stale. Run 'pnpm --filter @firemoot/admin build' and commit the result."
            exit 1
          fi

  codegen:
    name: SDK codegen drift
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up toolchain (mise)
        uses: jdx/mise-action@v4
        with:
          cache: true

      - name: Cache Coursier and sbt
        uses: actions/cache@v5
        with:
          path: |
            ~/.cache/coursier
            ~/.sbt
          key: sbt-${{ runner.os }}-${{ hashFiles('build.sbt', 'project/**', '.scalafmt.conf') }}
          restore-keys: sbt-${{ runner.os }}-

      - name: Cache pnpm store
        uses: actions/cache@v5
        with:
          path: ~/.local/share/pnpm/store
          key: pnpm-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: pnpm-${{ runner.os }}-

      - name: Install SDK dependencies
        run: pnpm install --frozen-lockfile

      - name: Regenerate OpenAPI document
        run: sbt -batch "server/runMain com.firemoot.OpenApiExport openapi.json"

      - name: Regenerate @firemoot/core
        run: pnpm --filter @firemoot/core run generate

      - name: Fail on drift
        run: |
          git add -A -- openapi.json sdk/ts/core/src/generated
          if ! git diff --cached --exit-code -- openapi.json sdk/ts/core/src/generated; then
            echo "::error::OpenAPI spec or generated SDK is stale. Run the codegen steps locally and commit the result."
            exit 1
          fi

  boot:
    name: Cold-boot gate (<15s)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up toolchain (mise)
        uses: jdx/mise-action@v4
        with:
          cache: true

      - name: Cache Coursier and sbt
        uses: actions/cache@v5
        with:
          path: |
            ~/.cache/coursier
            ~/.sbt
          key: sbt-${{ runner.os }}-${{ hashFiles('build.sbt', 'project/**', '.scalafmt.conf') }}
          restore-keys: sbt-${{ runner.os }}-

      - name: Build Docker image
        run: sbt -batch "server/Docker/publishLocal"

      - name: Boot the compose stack and assert healthy < 15s
        working-directory: deploy/compose
        run: |
          # Pre-pull the only remote image so the gate times boot, not the
          # registry download (firemoot:latest was just built locally).
          docker pull postgres:17
          start=$(date +%s)
          docker compose up -d
          healthy=0
          for _ in $(seq 1 150); do
            if curl -fsS localhost:6668/healthz >/dev/null 2>&1; then healthy=1; break; fi
            sleep 0.2
          done
          elapsed=$(( $(date +%s) - start ))
          echo "cold start to healthy: ${elapsed}s (healthy=${healthy})"
          curl -fsS localhost:6668/readyz || true
          docker compose logs firemoot || true
          docker compose down -v
          if [ "$healthy" -ne 1 ] || [ "$elapsed" -ge 15 ]; then
            echo "::error::cold start to healthy exceeded 15s (or never became healthy)"
            exit 1
          fi

  dogfood:
    name: SDK dogfood (real server)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up toolchain (mise)
        uses: jdx/mise-action@v4
        with:
          cache: true

      - name: Cache Coursier and sbt
        uses: actions/cache@v5
        with:
          path: |
            ~/.cache/coursier
            ~/.sbt
          key: sbt-${{ runner.os }}-${{ hashFiles('build.sbt', 'project/**', '.scalafmt.conf') }}
          restore-keys: sbt-${{ runner.os }}-

      - name: Cache pnpm store
        uses: actions/cache@v5
        with:
          path: ~/.local/share/pnpm/store
          key: pnpm-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: pnpm-${{ runner.os }}-

      - name: Build Docker image
        run: sbt -batch "server/Docker/publishLocal"

      - name: Pre-pull base images for Testcontainers
        run: docker pull postgres:17

      - name: Install SDK dependencies
        run: pnpm install --frozen-lockfile

      - name: Build SDK packages
        run: pnpm -r build

      # @firemoot/test boots firemoot:latest + Postgres via Testcontainers, seeds
      # it, and drives it with @firemoot/client - the dogfood gate (PLAN.md M4.5).
      - name: Run the dogfood suite against a real Firemoot
        env:
          FIREMOOT_DOGFOOD: "1"
        run: pnpm --filter @firemoot/test test
